<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:TDAH.Views.FlyoutUsers"
             x:Class="TDAH.Views.FlyoutUsers.FlyoutMenuPageUser"
             Title="Menu Nav">

    <StackLayout>
        <ListView x:Name="ListViewUser" x:FieldModifier="public">
            <ListView.ItemsSource>
                <x:Array Type="{x:Type local:FlyoutItemPageUser}">
                    <local:FlyoutItemPageUser Title="Inicio" TargetPage="{x:Type local:DashboardUser}"/>
                </x:Array>
            </ListView.ItemsSource>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Text="{Binding Title}"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Cerrar sesión" x:Name="BtnCerrarS" Command="{Binding CerrarSesionCommand}"/>
    </StackLayout>


</ContentPage>